import { createAction } from '@ngrx/store';

export const headerOpenUserVerificationDialog = createAction(
  '[Header] Open User Verification Dialog',
);
export const openUserVerificationDialogFromListBike = createAction(
  '[User List Bike] Open User Verification Dialog From List Bike',
);
