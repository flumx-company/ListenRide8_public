export enum ErrorHttpEnum {
  ERROR_UNAUTHORIZED_TOKEN_EXPIRED = 'Unauthorized', //'ERROR_UNAUTHORIZED_TOKEN_EXPIRED',
  ERROR_LOGIN_CREDENTIALS_INVALID = 'Invalid email or password.',
  HAVEN_T__TOKENS = 'Haven`t token',
}
